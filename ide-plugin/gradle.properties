org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m
org.gradle.caching=true
# The IDE brings its own Kotlin runtime, and a plugin that shipped a second one would load
# two of them into the same classloader.
kotlin.stdlib.default.dependency=false
